AUTOMATIC TRADING BOT
=====================

STATUS: Repo is PUBLIC + GitHub schedule enabled (Mon–Fri).

AUTOMATIC RUNS (no button clicks)
---------------------------------
GitHub starts the bot automatically on trading days:

  • 9:05 AM IST  — full session (~6.5 hours, scans every 5 min)
  • 2:50 PM IST  — backup session if morning was missed

You will see in Actions:
  • Event = "schedule" (not "workflow_dispatch")
  • Telegram: "Auto Trading Bot — RUNNING"

WEEKENDS: No runs (Saturday/Sunday). This is correct.

WHY YOU SAW "workflow_dispatch" ONLY
------------------------------------
That label appears on runs YOU started with "Run workflow".
Automatic runs show event = schedule on the next trading day morning.

MANUAL TEST (optional)
----------------------
Actions → Auto Trading Bot → Run workflow → max_minutes: 390

BACKUP (if schedule ever skips)
-------------------------------
cron-job.org → POST dispatch (see GitHub workflow ID 278548320)
Or run: powershell -File scripts\trigger_auto_session.ps1

REPO: https://github.com/SuvoG1250/telegram-trading-scanner
